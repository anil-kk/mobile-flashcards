import React from 'react'
import { StyleSheet, Text, View } from 'react-native'

const Card = () => {
    return (
        <View>
            <Text>New Card May be Modal, question, answer, true or false switch, submit</Text>
        </View>
    )
}

export default Card

const styles = StyleSheet.create({})
