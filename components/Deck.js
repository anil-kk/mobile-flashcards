import React from 'react'
import { StyleSheet, Text, View } from 'react-native'

const Deck = () => {
    return (
        <View>
            <Text>Deck, Title, total cards, add, start quiz</Text>
        </View>
    )
}

export default Deck

const styles = StyleSheet.create({})
